import styles from '../styles/Home.module.css';

export default function SectionLocal() {
  return (
    <section id="casal" className={`${styles.section} ${styles.casalSection}`}>
      
    </section>
  )
}